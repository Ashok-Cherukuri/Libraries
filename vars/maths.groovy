def sum(a,b)
{
  echo "the sum of ${a} , ${b} is ${a+b}"
}
def sub(a,b)
{
  echo "the sub of ${a} , ${b} is ${a-b}"
}
def mul(a,b)
{
  echo "the mul of ${a} , ${b} is ${a*b}"
}
def div(a,b)
{
  echo "the div of ${a} , ${b} is ${a/b}"
}
